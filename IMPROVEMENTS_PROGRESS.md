# IMPROVEMENTS.md progress tracker

Status of each item from [IMPROVEMENTS.md](./IMPROVEMENTS.md). Updated as work lands.

**Done: 25 / 25** (quick wins: 11/11, medium: 9/9, larger initiatives: 5/5)

**Note on browser tooling this session:** discovered mid-session (during item 20) that the browser automation's screenshot capture and actual click-coordinate space didn't match (screenshots rendered ~1447x840, real viewport was 1858x1079), which explains several early "click didn't register" retries logged in earlier items' commits - those weren't random flakiness, they were coordinate mismatches. Once found, later verification used element refs (from read_page) instead of raw coordinates, which worked reliably. If resuming manual verification in a future session, prefer ref-based clicks over coordinate guesses from the start.

**Note on browser verification:** items 22 (maximize) and 15 (docking) were built+tested but the dev-tooling screenshot capture was timing out at the time, so they only got build+test verification in the moment. Item 25 hit a real infinite-render-loop crash that only surfaced when actually rendered in a browser (a Zustand selector returning a fresh `[]` reference every call - see item 25's commit). Once the tooling recovered, items 22 and 15 were retroactively spot-checked by hand and both work correctly, but this is a reminder that "build passes, tests pass" is not the same guarantee as "a human clicked it" for anything involving a Zustand selector or other render-time state subscription.

## Quick Wins

| # | Item | Status | Notes |
|---|------|--------|-------|
| 1 | Self-host fonts (privacy claim) | **Done** | Google Fonts @import replaced with bundled @fontsource-variable Inter + JetBrains Mono; dist verified free of external font requests |
| 2 | Drop zone keyboard operability | **Done** | Enter/Space now opens the file picker |
| 3 | Topic-row buttons visible on keyboard focus | **Done** | group-focus-within added to the button strip and stats fade |
| 4 | Esc ordering (popovers before panels) | **Done** | New useEscapeToClose hook; export menu and 3D Display card consume Esc; panel-close undo toast not included (tracked as follow-up) |
| 5 | Light theme inside data surfaces (uPlot + 3D clear color + theme-color meta) | **Done** | New utils/chartTheme.ts; uPlot axes, trajectory canvas ink, 3D clear color, and the theme-color meta all follow the theme now; also fixed canvas font strings broken by the variable-font rename |
| 6 | "Copy link" button for the shareable URL layout | **Done** | Toolbar button copies location.href, 2s "Copied" confirmation |
| 7 | Replace transition-all with explicit properties | **Done** | Swept ~65 occurrences across 19 files; each one checked against its actual state branches (color-only -> transition-colors, opacity/transform/size toggles -> explicit property list) so nothing silently lost its fade/resize |
| 8 | Typography and copy consistency (ellipsis, casing, tabular-nums) | **Done** | All "..." -> "…" in UI strings; tabular-nums added to Toolbar stat values |
| 9 | Timeline slider focusable + scrub/bookmark hint | **Done** | tabIndex added (global arrow shortcuts already operate the playhead); title hints at double-click bookmarks |
| 10 | content-visibility on topic rows | **Done** | content-visibility: auto + contain-intrinsic-size on .topic-row |
| 11 | Narrow-viewport banner + timeline wrap | **Done** | Dismissible sm:hidden notice on DropZone; Timeline row gets flex-wrap |

## Medium Effort

| # | Item | Status | Notes |
|---|------|--------|-------|
| 12 | Shared PanelStates + OverlayCard (cohesion) | **Done** | PanelStates centralizes loading/error/empty chrome across panels. New shared OverlayCard centralizes solid, subtle, and surface canvas-overlay chrome while leaving positioning and content styles at call sites; adopted by the 3D Display controls and scene readout, trajectory readout, and CameraInfo badge |
| 13 | 3D Display card progressive disclosure | **Done** | Only color-by/point-size/grid/axes visible by default; rest behind 3 native `<details>` sections (Coordinate frame, Range and clipping, Accumulation, Overlays) with expand-state persisted per-panel in threeDPanelStore; defaults save/reset moved to a pinned footer; manually verified in-browser (dark + light theme, expand-state survives close/reopen) |
| 14 | Timeline data-density strip | **Done** | Reused existing readAllMessageStats (built for Bag Health, already works across mcap/db3/bag) - no new parser code needed. New utils/messageDensity.ts (pure, unit-tested bucketing) + hooks/useMessageDensity.ts + a canvas strip in Timeline.tsx. Verified rendering in-browser |
| 15 | Docking discoverability + keyboard path | **Done** | 6-dot grip glyph on every header; first-drag-of-session dim on sibling panels (dragDockStore.hasDraggedOnce); panel headers focusable, Alt+Arrow moves the focused panel via new layoutStore.movePanel (wraps dockPanel using the existing traversal order). Tests cover movePanel; browser click-through blocked by the tooling issue noted in item 22 |
| 16 | Sample bag lands on a curated cockpit layout | **Done** | SampleBagButton awaits load then builds H(3d-scan, V(image, plot)) via openPanel/dockPanel (exported panelLeafId), seeks 3s in, starts playback. One-time "click a topic..." hint over the sidebar, dismissal persisted to localStorage. Verified in-browser including that the hint stays dismissed across a second sample-bag load |
| 17 | Live status: consolidate buttons, aria-live, buffer extent | **Done** | Removed duplicate Toolbar FollowLiveButton; colorblind-safe solid/hollow dot shape in both remaining dots; reconnect attempt count + delay surfaced ("Retrying in 8s (attempt 3)"); aria-live region in Timeline; "Back to live" pill on the track. Deliberately skipped the buffer-extent tint - traced useLivePlayhead.ts and found the visible track already IS the buffer extent at all times for a live bag, so there's no distinct sub-region to tint. Verified via a real failing ws:// connection through several reconnect cycles (console clean, exact expected message rendered); screenshot tooling was down so dot/pill styling wasn't eyeballed, only the text-driven state machine |
| 18 | Error copy with next steps | **Done** | Bag-load failures now become structured errors with preserved raw details and recovery actions for remote CORS/Range configuration, unsupported formats, incomplete/corrupt files, and retryable URL failures. Shared panel errors detect missing-schema failures and open the existing schema-paste modal with the panel's type/topic/bag context; unknown failures retain Copy error details. Four classifier tests added |
| 19 | Modal focus trap | **Done** | Tab/Shift+Tab now wrap inside the dialog; no component-test harness exists in this repo (all tests are logic-only .ts), so no automated test was added for this one |
| 20 | Micro-interactions motion spec (CSS-only) | **Done** | Panel open/close (new panel-in/panel-out keyframes, close deferred behind exit animation), dock-drop pulse (dragDockStore.markDropped), Timeline knob scale + hover/drag time tooltip, discrete-seek width transition (new playheadStore.discreteSeekId, distinguishes seek() from tick()/seekFraction()). Found and fixed a latent bug: the shared .progress-bar class had an unconditional width transition that had been lagging the Timeline fill behind the real playhead during every RAF frame - removed it from the shared class, applied directly at DropZone's call site instead. All CSS-only, no new dependency, everything respects the existing reduced-motion global kill. Partial browser verification (viewport/screenshot coordinate mismatch discovered and worked around mid-session, see commit); close-animation and sample-bag layout re-confirmed working post-change with zero console errors |

## Larger Initiatives (design decisions collected 2026-07-16)

| # | Item | Decision | Status | Notes |
|---|------|----------|--------|-------|
| 21 | Visual identity: instrument vs glass | Instrument workspace, branded landing | **Done** | PanelGrid and PanelShell now solid/opaque, no backdrop-filter, no bg-grid/gradient behind panels; DropZone untouched. Verified in-browser |
| 22 | Panel maximize (then maybe tabs) | Maximize only | **Done** | Double-click header or button toggles maximizedId in layoutStore; auto-clears on close/closeAll/closePanelsForBag/restoreLayout; Esc restores before falling through to panel-close. tests/store/layoutStore.test.ts covers every branch. Browser click-through verification hit tooling issues this round (see commit); verified via build + full test suite + manual trace instead |
| 23 | Named layout presets | Type-based slots, no reassignment prompt | **Done** | Toolbar Presets menu saves the current split tree as named panel-kind/message-type slots in localStorage, then applies it to the focused bag by matching the first unused topic of each type. Missing topics drop silently and splits collapse; presets can be deleted. Five store tests cover serialization, focused-bag remapping, missing-slot collapse, deletion, and persistence. Full 553-test suite, build, and lint pass; focused test rerun after final accessibility refinements |
| 24 | 3D/canvas accessibility tier | Tier 1 (keyboard camera, live-region summary, aria-label, hidden data table) | **Done** | 3D Scene: orbitBy/zoomBy on SceneRefs (I/J/K/L orbit, +/- zoom, F re-fit), avoiding arrow keys/Space already bound globally; orbit math mirrors OrbitControls' own quaternion technique since raw THREE.Spherical assumes Y-up but this scene is Z-up; canvas is focusable with role="img", descriptive aria-label, and a live region announcing scene kind/frame/point count. TimeSeriesPlot: visually-hidden table with min/max/last per visible series (raw fields + expressions). Full build+test+lint pass, and browser-verified directly: each key confirmed to move the camera or update the live table by diffing screenshots and reading the DOM |
| 25 | Per-topic timeline lanes | Not asked separately - proceeding with my stated recommendation (expandable drawer, pinned topics, capped ~8 lanes), since user has consistently taken the recommended option | **Done** | New pinnedTopicsStore + pin toggle in TopicRow + expandable drawer in Timeline with per-topic density lanes, playhead line, unpin. Found and fixed a real infinite-render-loop bug during browser verification (Zustand selector returning a fresh `[]` on every call) - see commit for the full story and why it's a reminder that build+test-only verification (used for items 22/15 due to a tooling outage) isn't equivalent to an actual render check |
