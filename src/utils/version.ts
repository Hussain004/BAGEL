/**
 * Single source of truth for the BAGEL release version.
 *
 * Surfaced in the toolbar logo tooltip and the About modal so the user can
 * always tell which build is live.
 */
export const APP_VERSION = '1.3.3';
